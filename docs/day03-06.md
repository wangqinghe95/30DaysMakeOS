# Day03-06

```
haribote.img: ipl.bin haribote.sys
	# Create an empty 1.44MB floppy image
	dd if=/dev/zero of=$@ bs=512 count=2880

	# Format the image as FAT12
    # :: means disk A
	mformat -f 1440 -i $@ ::

	# Write the boot sector
	dd if=ipl.bin of=$@ bs=512 count=1 conv=notrunc

    # ::/ means root folder
	mcopy -i $@ haribote.sys ::/
```

查验是否正确执行

```
// bash

make run-debug
x /16bx 0x2c00
```

结果

```
qemu-system-x86_64 -drive file=haribote.img,format=raw,if=floppy -boot a -monitor stdio
QEMU 6.2.0 monitor - type 'help' for more information
(qemu) x /16bx 0xc200
000000000000c200: 0xf4 0xeb 0xfd 0x00 0x00 0x00 0x00 0x00
000000000000c208: 0x00 0x00 0x00 0x00 0x00 0x00 0x00 0x00
```

说明0x2c00有数据



