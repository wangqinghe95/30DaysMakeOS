# 30DaysMakeMyOS

## Day02

Day2的代码，也就是 helloos.asm 完成了一个简单的汇编启动代码，和项目构建执行命令。

## Day03

### 01 制作真正的IPL

读取磁盘，实现真正的操作系统第一步

### 02 试错


### 03 读18个扇区

### 04 写入10个柱面

### 05 着手开发操作系统

### 06 从启动区执行操作系统

### 07 确认操作系统执行情况

设置显卡模式，将画面切换成一片漆黑

### 08 32位模式前期准备

### 09 开始导入C语言

添加 bootpack.c 引入 C 文件

### 10 实现HLT（harib00j）

在 naskfunc.asm 文件中添加 hlt 汇编代码

## Day04 C语言与画面显示的练习

### 01 用C语言实现内存写入（harib01a）

naskfunc.asm 文件添加 write_mem8 汇编代码，修改 VGA 内存数据，将窗口渲染成全白
验证前面的文件结构的地址分配，Make 文件正常运行

### 02 条纹图案（harib01b）

修改 VGA 内存数据，将窗口修改成条纹图案

### 03 挑战指针（harib01c）

介绍在寄存器的视角下，指针的以及指针指向的内存的区别和联系

### 04 指针的应用（1）（harib01d）

删掉 write_mem8 的实现，使用指针直接访问 VGA 内存改变内存数据

### 05 指针的应用（2）（harib01e）

探讨 *p 和 p[i] 的区别

### 06 色号设定（harib01f）

设置 rgb 颜色

### 07 绘制矩形（harib01g）

绘制不同颜色的矩阵

### 08 今天的成果（harib01h）

图形页面的雏形

## Day05 结构体、文字显示与GDT/IDT初始化

### 01 接收启动信息（harib02a）

调整窗口页面设置属性函数

### 02 试用结构体（harib02b）

使用结构体保存数据

### 03　试用箭头记号（harib02c）

指针引用的方法至二——箭头

### 04　显示字符（harib02d）

窗口打印打印字符 'A' —— 使用二进制0/1利用图像像素实现打印效果

### 05 增加字体（harib02e）

引入 hankaku.txt 字体文件
实验打印 ABC 123 符号

### 06　显示字符串（harib02f）

实现一个显示字符串的函数
修改代码中关于warn的警告（unsigned char* 和 char* 函数传参问题）

### 07 显示变量值（harib02g）

将变量值显示到窗口中便于调试
实现了本地版本的stdio.c文件
实现了 sprintf 函数 

### 08　显示鼠标指针（harib02h）

在窗口中显示鼠标图案

### 09 GDT与IDT的初始化（harib02i）

GDT: global descriptor table register，全局段号记录表
IDT：interrupt descriptor table，中断几率表

## Day06

### 01 && 02 && 03

这三小节内容就是将一个大文件分割成多个小文件，属于一个整体内容，放在一起

01 将 bootpack.c 代码分割成 dectbl.c 和 graphic.c 内容
02 修改 Makefile 适配项目内容
03 整理头文件，将三个c文件共同需要使用的函数声明和全局变量放入到 bootpack.h 文件中

### 05 初始化PIC（harib03d）

添加中段向量表

### 06 中断处理程序的制作（harib03e）

## Day07

### 01 获取按键编码（hiarib04a）

屏幕上显示键盘按键的编码（监听键盘事件）

### 02 加快中断处理（hiarib04b）

中断处理程序只负责接受数据
处理请求放到主函数中进行
目的是为了防止在中段处理进行过程中太耗时，导致其他事件无法监听

### 03 制作FIFO缓冲区（hiarib04c）

使用32位数组来保持一个监听事件事件中的传送过来的多个数据

### 04 改善FIFO缓冲区（hiarib04d)

使用循环数组来使用缓存区数组
